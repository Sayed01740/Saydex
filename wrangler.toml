# Cloudflare Pages / Workers configuration for Saydex Protocol
name = "saydex"
compatibility_date = "2024-09-25"
pages_build_output_dir = "dist"

[vars]
ENVIRONMENT = "production"

# Observability and edge logs
[observability]
enabled = true
